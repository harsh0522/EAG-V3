[project]
name = "ai-prompt-builder-evaluator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic",
    "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
